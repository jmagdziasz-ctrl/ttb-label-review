fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
Pillow>=10.0
numpy>=1.26
rapidocr-onnxruntime>=1.2

# Powers the Claude Vision extractor - only actually used if ANTHROPIC_API_KEY
# is set server-side, or a caller supplies their own key (see the "Reading
# settings" panel in the UI). The free/offline OCR path works without it.
anthropic>=0.40
